import {observer} from "mobx-react-lite";
import {Container} from "@mui/material";

const Day = observer((props) => {

    return (
        <Container sx={{mt: 2}}>
        </Container>
    );
});

export default Day;