import {observer} from "mobx-react-lite";
import {Container} from "@mui/material";

const Week = observer((props) => {

    return (
        <Container sx={{mt: 2}}>
        </Container>
    );
});

export default Week;